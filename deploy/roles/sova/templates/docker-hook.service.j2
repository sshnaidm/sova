[Unit]
Description=Docker hook service

[Service]
ExecStart=/bin/bash -c '/usr/bin/python {{ docker_hook }} -t {{ auth_token }} -c {{ redeploy_script }} --port {{ hook_port }} &> {{ docker_hook_log }}'

[Install]
WantedBy=multi-user.target
